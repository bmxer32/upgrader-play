# Обход открыт целиком, и это осознанно.
#
# Страницу приглашения /i/ закрывает мета-тег noindex в самой странице.
# Запрет здесь работал бы не «вдобавок», а вместо: роботу, которому
# запрещён обход, неоткуда прочитать noindex — он не заходит на страницу.
# Такой адрес всё равно может попасть в выдачу по внешней ссылке, только
# уже без нашего запрета на показ. Поэтому обход разрешаем, а показ
# запрещаем тем единственным способом, который робот увидит.
#
# Адреса с параметрами (?c=, ?ref=, ?utm_*) сводит canonical на страницах.

User-agent: *
Allow: /

Sitemap: https://upgrader-play.ru/sitemap.xml
